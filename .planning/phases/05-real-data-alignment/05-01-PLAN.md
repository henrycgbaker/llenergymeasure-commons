---
phase: 05-real-data-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/src/routes/+page.svelte
  - platform/src/routes/+page.ts
  - platform/src/routes/methodology/+page.svelte
  - platform/src/lib/components/charts/HeatmapFilters.svelte
autonomous: true
requirements: [NARR-01, NARR-03, NARR-05, DATA-01, DATA-02, DATA-08]

must_haves:
  truths:
    - "Page title and meta description show the dynamically computed energy ratio, not hardcoded '8x'"
    - "Beat 3 prose names only backends present in the data (pytorch, vllm) - no TensorRT"
    - "Beat 5 bar chart highlights the actual lowest-energy backend with the efficiency colour"
    - "Beat 5 deployment prose references only vLLM, not TensorRT"
    - "Methodology page states 37 records, Qwen/Qwen2.5-0.5B, A100-PCIE-40GB (40GB, 250W), pytorch + vllm"
    - "Methodology fixture-notice says real measurements, not simulated"
    - "HeatmapFilters backendLabels has no dead tensorrt entry"
  artifacts:
    - path: "platform/src/routes/+page.svelte"
      provides: "Dynamic title/meta, updated Beat 3/5 prose, dynamic bar-fill--best"
      contains: "data.energyRatio"
    - path: "platform/src/routes/+page.ts"
      provides: "bestBackend derivation returned alongside deploymentData"
      exports: ["load"]
    - path: "platform/src/routes/methodology/+page.svelte"
      provides: "Accurate coverage statement, real-data fixture notice"
      contains: "37"
    - path: "platform/src/lib/components/charts/HeatmapFilters.svelte"
      provides: "Clean backendLabels without dead tensorrt entry"
  key_links:
    - from: "platform/src/routes/+page.ts"
      to: "platform/src/routes/+page.svelte"
      via: "data.bestBackend passed from load to template"
      pattern: "data\\.bestBackend"
    - from: "platform/src/routes/+page.svelte"
      to: "svelte:head title/meta"
      via: "data.energyRatio template expression"
      pattern: "data\\.energyRatio.*x"
---

<objective>
Align all narrative copy, page metadata, methodology content, and filter labels with the real
37-record measurement dataset (pytorch + vllm only, Qwen/Qwen2.5-0.5B, A100-PCIE-40GB).

Purpose: The data was updated in commit ac1e4fd but narrative copy, metadata, and methodology
still reference the old synthetic fixture (8x ratio, TensorRT, 180 records, Llama 3, A100 80GB).
This plan makes every user-facing claim consistent with the actual data.

Output: Four updated source files with no stale data references. Build passes, types check.
</objective>

<execution_context>
@/Users/henrybaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henrybaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-real-data-alignment/05-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From platform/src/routes/+page.ts (current load return shape):
```typescript
return {
  allResults,        // ExperimentResult[]
  heatmapCells,      // HeatmapCell[]
  worstEnergy,       // number
  bestEnergy,        // number
  energyRatio,       // number (Math.round(worst/best) for default heatmap slice)
  worstCharges,      // number
  bestCharges,       // number
  worstResult,       // ExperimentResult
  bestResult,        // ExperimentResult
  deploymentData,    // { backend: string; energyPerToken: number; label: string }[]
  minEnergyGlobal,   // number
  maxEnergyGlobal,   // number
  globalRatio,       // number
  defaultBackend,    // string
  backends           // string[]
};
```

From platform/src/lib/data/types.ts:
```typescript
export interface ExperimentResult {
  // ... fields including:
  backend: string;
  avg_energy_per_token_j: number;
  effective_config: {
    model: string;
    backend: string;
    precision: string;
    dimensions: Record<string, unknown>;
  };
  environment_snapshot: Record<string, unknown> | null;
}
```
</interfaces>

**Data facts (verified from fixture-results.json):**
```
Records: 37
Model: Qwen/Qwen2.5-0.5B (from effective_config.model)
GPU: NVIDIA A100-PCIE-40GB (40 GB VRAM, 250 W power limit)
Backends: pytorch (24 records), vllm (13 records)
PyTorch dimensions: precision (bf16, fp16), batch_size (1, 4, 8, 16), attn_implementation (eager, sdpa, flash_attention_2)
vLLM dimensions: precision (bf16 only), max_num_seqs (64, 128, 256), enforce_eager (true, false), enable_chunked_prefill (true, false)
Energy range: 0.0193 - 1.1625 J/token
Global ratio: 60x
PyTorch heatmap slice ratio: 13x (this is data.energyRatio)
Best backend overall: vllm (0.0193 J/token)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix page metadata + Beat 3/5 narrative copy + bestBackend derivation</name>
  <files>platform/src/routes/+page.ts, platform/src/routes/+page.svelte</files>
  <action>
**In +page.ts:**

1. After `deploymentData` is built (around line 64), derive `bestBackend`:
```typescript
const bestBackend =
  deploymentData.length > 0
    ? deploymentData.reduce((a, b) => (a.energyPerToken < b.energyPerToken ? a : b)).backend
    : '';
```

2. Add `bestBackend` to the return object (after `deploymentData`).

3. Remove the dead `tensorrt: 'TensorRT'` entry from the `labelMap` inside `deploymentData` (line 57). Only keep `pytorch: 'PyTorch'` and `vllm: 'vLLM'`.

**In +page.svelte:**

4. Replace the hardcoded title (line 167):
   FROM: `<title>LLM Energy Commons — Same model. Same GPU. 8x energy difference.</title>`
   TO: `<title>LLM Energy Commons — Same model. Same GPU. {data.energyRatio}x energy difference.</title>`

5. Replace the hardcoded meta description (line 170):
   FROM: `content="Same model, same GPU — up to 8x energy difference from configuration choices alone. Evidence-based guidance for AI energy policy."`
   TO: `content="Same model, same GPU — up to {data.energyRatio}x energy difference from configuration choices alone. Evidence-based guidance for AI energy policy."`

6. Update Beat 1 comment (line 177) from `8x counter` to `ratio counter` (cosmetic).

7. Update Beat 3 intro body (line 272):
   FROM: `Filter by inference backend (PyTorch, vLLM, TensorRT) and attention implementation.`
   TO: `Filter by inference backend (PyTorch, vLLM) and attention implementation.`

8. Update Beat 5 Lever 2 deployment body (line 406):
   FROM: `Switching to vLLM or TensorRT at the same precision reduces energy per token materially`
   TO: `Switching to vLLM at the same precision reduces energy per token materially`

9. Fix `bar-fill--best` (line 420):
   FROM: `class:bar-fill--best={item.backend === 'tensorrt'}`
   TO: `class:bar-fill--best={item.backend === data.bestBackend}`

10. Update Beat 5 expandable detail (lines 433-436):
   FROM: `vLLM and TensorRT achieve efficiency gains through continuous batching, kernel fusion, and memory management optimisations absent in vanilla PyTorch. These gains compound: at higher batch sizes, TensorRT's fused attention kernels substantially reduce memory bandwidth pressure compared to eager or SDPA attention.`
   TO: `vLLM achieves efficiency gains through continuous batching, PagedAttention memory management, and kernel optimisations absent in vanilla PyTorch. These gains compound at higher concurrency: vLLM's server-level scheduling amortises GPU overhead across concurrent sequences more effectively than per-request PyTorch inference.`

11. Update the Beat 3 procurement comment (line 464) from `8x cost framing` to `cost framing` (cosmetic).

12. In the Lever 1 regulation expandable detail (line 382), update the hardware reference:
   FROM: `a fixed hardware reference (e.g., an A100 80GB)`
   TO: `a fixed hardware reference (e.g., an A100 40GB)`
  </action>
  <verify>
    <automated>cd /Users/henrybaker/Repositories/llem-commons/platform && npm run check && npm run build && grep -c "8x" build/index.html | grep "^0$" && grep "energyRatio" build/index.html | head -1</automated>
  </verify>
  <done>
  - Title and meta description use dynamic {data.energyRatio}x, not hardcoded "8x"
  - Beat 3 prose names only PyTorch and vLLM
  - Beat 5 bar chart uses data.bestBackend for efficiency highlight (resolves to 'vllm')
  - Beat 5 prose references vLLM only, no TensorRT
  - +page.ts returns bestBackend alongside deploymentData
  - No occurrence of "8x" remains in the built index.html
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite methodology page for real data</name>
  <files>platform/src/routes/methodology/+page.svelte</files>
  <action>
Update the methodology page to match the real 37-record dataset. All changes are to static HTML/Svelte content. No +page.ts needed (per research anti-pattern guidance).

1. **"The short version" section** (line 28-29):
   FROM: `can draw up to <strong>8 times more energy</strong> depending on four configuration choices: numerical precision, batch size, inference backend, and attention implementation`
   TO: `can draw up to <strong>60 times more energy</strong> depending on configuration choices: numerical precision, batch size, inference backend, and attention implementation`

2. **"What we measure" config dimensions - Batch size** (lines 96-100):
   FROM: `Batch size 1 (processing one prompt at a time) is the worst-case scenario for energy efficiency. Batch size 128 can be 2-3 times more efficient per token than batch size 1.`
   TO: `Batch size 1 (processing one prompt at a time) is the worst-case scenario for energy efficiency. Batch size 16 can be significantly more efficient per token than batch size 1.`
   (The real data sweeps batch_size 1, 4, 8, 16 - not up to 128.)

3. **"What we measure" config dimensions - Inference backend** (lines 106-112):
   FROM: `We test three backends: <strong>PyTorch</strong> (the standard research framework, highest baseline energy), <strong>vLLM</strong> (production-optimised serving with PagedAttention memory management), and <strong>TensorRT-LLM</strong> (NVIDIA's compiled inference engine, lowest energy in most configurations). Backend choice can account for a 30-50% difference in energy consumption for the same model and precision.`
   TO: `We test two backends: <strong>PyTorch</strong> (the standard research framework, with per-request batch inference) and <strong>vLLM</strong> (production-optimised serving with PagedAttention memory management and continuous batching). Backend choice is the single largest factor in energy consumption — vLLM achieves up to 5x lower energy per token than the best PyTorch configuration at the same precision.`

4. **Add a note about vLLM dimensions** after the Attention implementation config-item (after line 125), add a new paragraph:
   ```html
   <p>
     Note: vLLM uses server-level configuration dimensions (maximum concurrent sequences,
     chunked prefill, eager vs. graph mode) rather than per-request batch size. These
     dimensions are specific to serving frameworks and do not have direct PyTorch equivalents.
   </p>
   ```

5. **"Hardware" section** (lines 179-184):
   FROM: `The data on this site was collected on a single NVIDIA A100 80GB GPU. The A100 is a high-end data-centre GPU representative of the hardware used for production LLM inference in cloud environments. Its 400W thermal design power (TDP) and 80GB of HBM2e memory make it capable of running large models at full precision without the memory constraints that affect smaller GPUs.`
   TO: `The data on this site was collected on a single NVIDIA A100-PCIE-40GB GPU (40 GB VRAM, 250 W power limit, CUDA 12.2). The A100 is a data-centre GPU representative of the hardware used for production LLM inference in cloud environments.`

6. **"About the data" coverage - What is covered** (lines 200-208):
   Replace the entire `<ul>` inside `.coverage-item.covered`:
   ```html
   <ul>
     <li>Model: <strong>Qwen/Qwen2.5-0.5B</strong> (Alibaba)</li>
     <li>Hardware: <strong>NVIDIA A100-PCIE-40GB</strong></li>
     <li>PyTorch precisions: <strong>bf16, fp16</strong></li>
     <li>PyTorch batch sizes: <strong>1, 4, 8, 16</strong></li>
     <li>Backends: <strong>PyTorch, vLLM</strong></li>
     <li>Attention: <strong>eager, SDPA, Flash Attention v2</strong> (PyTorch)</li>
     <li>Total configurations: <strong>37 unique experiments</strong></li>
   </ul>
   ```

7. **"About the data" coverage - What is not covered** (lines 213-220):
   Replace the `<ul>` inside `.coverage-item.not-covered`:
   ```html
   <ul>
     <li>Other models (Llama, Mistral, Gemma, larger Qwen variants)</li>
     <li>Other GPU hardware (H100, A100-80GB, RTX series, AMD)</li>
     <li>Other backends (TensorRT-LLM, ONNX Runtime)</li>
     <li>Multi-GPU configurations</li>
     <li>Training workloads (inference only)</li>
     <li>Quantisation methods (GPTQ, AWQ, int8)</li>
     <li>Speculative decoding and KV-cache strategies</li>
     <li>Network latency or serving overhead</li>
   </ul>
   ```

8. **Fixture notice** (lines 226-232):
   FROM: `<strong>Current data status: Preview (simulated).</strong> The data on this site is simulated fixture data modelled on the llenergymeasure schema, designed to demonstrate realistic magnitudes and directional trends. The 8x worst-to-best ratio is calibrated to match expected empirical results. Real measurements on physical hardware are in progress and will replace this fixture data when available.`
   TO: `<strong>Current data status: Real measurements (v1, 2026-03-26).</strong> The data on this site comprises 37 experiments measured on a single NVIDIA A100-PCIE-40GB GPU using the llenergymeasure benchmark protocol. Measurements are reproducible by running the same configuration sweep with the open-source llenergymeasure tool.`

9. **Limitations - "Snapshot in time"** (line 258-259):
   FROM: `Software backends are updated frequently. vLLM and TensorRT-LLM efficiency improvements in future releases would reduce the measured values for those backends.`
   TO: `Software backends are updated frequently. vLLM efficiency improvements in future releases would change the measured values for that backend.`

10. **Limitations - "English prompts"** (line 248): Keep as-is - still accurate.

11. **Technical detail expandable** (line 165): Update mention of "full environment snapshot" to clarify it is recorded but not published:
    FROM: `a full environment snapshot (GPU model, driver version, CUDA version, Python version, library versions)`
    TO: `an environment snapshot (GPU model, driver version, CUDA version, Python version, library versions) recorded at measurement time`
  </action>
  <verify>
    <automated>cd /Users/henrybaker/Repositories/llem-commons/platform && npm run check && npm run build && grep -c "TensorRT" build/methodology.html | grep "^[01]$" && grep "37" build/methodology.html | head -1</automated>
  </verify>
  <done>
  - Methodology page states 37 records, Qwen/Qwen2.5-0.5B, A100-PCIE-40GB, pytorch + vllm
  - No TensorRT listed under "What is covered" (may appear once under "What is not covered" or limitations)
  - Fixture notice says "Real measurements" not "simulated"
  - Hardware section says 40GB/250W not 80GB/400W
  - vLLM-specific dimensions briefly noted
  - Build passes with no type errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove dead tensorrt entry from HeatmapFilters + run full CI suite</name>
  <files>platform/src/lib/components/charts/HeatmapFilters.svelte</files>
  <action>
1. In HeatmapFilters.svelte line 37, remove the `tensorrt: 'TensorRT'` entry from `backendLabels`:
   FROM:
   ```typescript
   const backendLabels: Record<string, string> = {
     pytorch: 'PyTorch',
     vllm: 'vLLM',
     tensorrt: 'TensorRT'
   };
   ```
   TO:
   ```typescript
   const backendLabels: Record<string, string> = {
     pytorch: 'PyTorch',
     vllm: 'vLLM'
   };
   ```

2. Run the full CI verification suite to confirm everything passes:
   - `npm run check` (svelte-check + TypeScript)
   - `npx vitest run` (unit tests)
   - `npm run lint` (ESLint)
   - `npm run build` (static build)

3. Verify no stale references remain across the entire codebase:
   - `grep -r "tensorrt" platform/src/` should return zero hits in prose/labels (may appear in types or test fixtures)
   - `grep -r '"8x"' platform/src/routes/` should return zero hits
  </action>
  <verify>
    <automated>cd /Users/henrybaker/Repositories/llem-commons/platform && npx vitest run && npm run lint && npm run check && npm run build</automated>
  </verify>
  <done>
  - HeatmapFilters backendLabels contains only pytorch and vllm
  - Full CI suite passes: vitest, eslint, svelte-check, build
  - No hardcoded "8x" or "tensorrt" in narrative/label code (prose/UI strings)
  </done>
</task>

</tasks>

<verification>
After all tasks:
1. `cd platform && npm run build` succeeds
2. `grep -c '"8x"' build/index.html` returns 0
3. `grep "TensorRT" build/index.html` returns 0 hits (no TensorRT in main narrative)
4. `grep "37" build/methodology.html` returns at least 1 hit (record count)
5. `grep "simulated" build/methodology.html` returns 0 hits
6. `grep "bestBackend" platform/src/routes/+page.ts` confirms new field is exported
</verification>

<success_criteria>
- All energy ratio claims use data.energyRatio (dynamic), not "8x" (hardcoded)
- TensorRT removed from all narrative prose, filter labels, and bar chart logic
- Beat 5 bar chart highlights vllm (actual best backend) with efficiency colour
- Methodology page accurately describes 37 records, 2 backends, correct model/GPU/VRAM
- Fixture notice updated from "simulated" to "real measurements"
- Full CI suite green: vitest, eslint, svelte-check, build
</success_criteria>

<output>
After completion, create `.planning/phases/05-real-data-alignment/05-01-SUMMARY.md`
</output>
