---
phase: 04-distribution-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - platform/src/routes/chart/[slug]/+page.svelte
  - platform/src/routes/chart/[slug]/+page.ts
  - platform/src/routes/embed/[slug]/+page.svelte
  - platform/src/routes/embed/[slug]/+page.ts
  - platform/src/lib/components/CitationBlock.svelte
  - platform/src/lib/components/EmbedCodeBlock.svelte
  - platform/src/lib/components/ChartExportButton.svelte
  - platform/src/routes/+page.svelte
  - scripts/generate-og-images.ts
  - platform/static/og/
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "Each of the 5 chart types has a working permalink at /chart/{slug}"
    - "Each chart page shows the interactive chart, title, description, methodology link, citation block, embed code, and download buttons"
    - "Each chart page has correct Open Graph and Twitter Card meta tags with a static preview image"
    - "Each of the 5 chart types has a minimal embed page at /embed/{slug}"
    - "Embed pages send postMessage resize events to parent frames"
    - "User can copy iframe embed code with one click"
    - "User can copy BibTeX or APA citation with one click"
    - "User can download chart as PNG (all charts) or SVG (D3 charts only)"
    - "Exported PNG/SVG includes chart title at top and 'Henry Baker . Hertie School' attribution at bottom"
    - "Filter state from URL query parameters is restored on chart/embed page load"
    - "Fragment IDs on the scroll narrative page enable in-context linking"
    - "Static OG preview images exist for all 5 charts at build time"
  artifacts:
    - path: "platform/src/routes/chart/[slug]/+page.svelte"
      provides: "Full chart context page with all distribution features"
      min_lines: 80
    - path: "platform/src/routes/chart/[slug]/+page.ts"
      provides: "Data loader with entries() for prerendering all 5 slugs"
      exports: ["entries", "load"]
    - path: "platform/src/routes/embed/[slug]/+page.svelte"
      provides: "Minimal embed page with attribution bar and resize observer"
      min_lines: 40
    - path: "platform/src/routes/embed/[slug]/+page.ts"
      provides: "Data loader with entries() for prerendering all 5 embed pages"
      exports: ["entries", "load"]
    - path: "platform/src/lib/components/CitationBlock.svelte"
      provides: "BibTeX/APA citation display with copy buttons"
    - path: "platform/src/lib/components/EmbedCodeBlock.svelte"
      provides: "Iframe embed code display with copy button"
    - path: "platform/src/lib/components/ChartExportButton.svelte"
      provides: "PNG/SVG download buttons wrapping title + chart + attribution for export"
  key_links:
    - from: "platform/src/routes/chart/[slug]/+page.ts"
      to: "platform/src/lib/data/chartMeta.ts"
      via: "entries() iterates CHART_SLUGS"
      pattern: "CHART_SLUGS\\.map"
    - from: "platform/src/routes/chart/[slug]/+page.svelte"
      to: "platform/src/lib/data/citations.ts"
      via: "CitationBlock receives citation functions"
      pattern: "chartApa|chartBibtex"
    - from: "platform/src/routes/embed/[slug]/+page.svelte"
      to: "window.parent.postMessage"
      via: "ResizeObserver sends height to parent"
      pattern: "postMessage.*llem-resize"
    - from: "ChartExportButton.svelte"
      to: "platform/src/lib/utils/chartExport.ts"
      via: "exportChartAsPng receives the outer export wrapper div (title + chart + attribution), not the raw chart element"
      pattern: "exportChartAsPng\\(exportContainer"
---

<objective>
Create the 10 prerendered route pages (5 chart + 5 embed) that make every chart independently shareable, embeddable, and citable. Each chart page combines the existing interactive chart component with new distribution controls (citation block, embed code, export buttons).

Purpose: A policy team member or journalist can link to, embed, cite, or download any chart without contacting the authors.
Output: /chart/{slug} pages with full context, /embed/{slug} pages for iframes, reusable CitationBlock, EmbedCodeBlock, and ChartExportButton components, static OG preview images.
</objective>

<execution_context>
@/Users/henrybaker/.claude/get-shit-done/workflows/execute-plan.md
@/Users/henrybaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-distribution-polish/04-CONTEXT.md
@.planning/phases/04-distribution-polish/04-RESEARCH.md
@.planning/phases/04-distribution-polish/04-01-SUMMARY.md

@platform/src/lib/data/types.ts
@platform/src/routes/explorer/+page.ts
@platform/src/routes/explorer/+page.svelte
@platform/src/routes/+layout.ts
@platform/svelte.config.js

<interfaces>
<!-- From Plan 01 outputs (chartMeta.ts, citations.ts, chartExport.ts) -->

From platform/src/lib/data/chartMeta.ts (created in Plan 01):
```typescript
export const CHART_SLUGS = ['heatmap', 'timeseries', 'surface', 'pca', 'parallel-coords'] as const;
export type ChartSlug = typeof CHART_SLUGS[number];
export const CHART_META: Record<ChartSlug, { title: string; description: string; altText: string }>;
```

From platform/src/lib/data/citations.ts (created in Plan 01):
```typescript
export const PLATFORM_APA: string;
export const PLATFORM_BIBTEX: string;
export function chartApa(title: string, slug: string): string;
export function chartBibtex(title: string, slug: string): string;
```

From platform/src/lib/utils/chartExport.ts (created in Plan 01):
```typescript
export function exportChartAsPng(containerEl: HTMLElement, filename: string): Promise<void>;
export function exportChartAsSvg(svgEl: SVGSVGElement, filename: string): Promise<void>;
export function exportPlotlyPng(plotDiv: HTMLElement, filename: string, plotly: unknown): Promise<void>;
```

From existing codebase:
```typescript
// platform/src/routes/explorer/+page.ts pattern
export const load: PageLoad = async ({ fetch }) => { /* fetches fixture-results.json + pca-projection.json */ };

// platform/src/routes/+layout.ts
export const prerender = true;
export const trailingSlash = 'always';
```

Existing chart components (all in platform/src/lib/components/charts/):
- ConfigHeatmapInteractive.svelte -- full interactive heatmap
- PowerTimeseries.svelte -- SVG timeseries with scroll/manual playback
- Surface3D.svelte -- Plotly 3D surface
- PCAProjection.svelte -- Plotly 3D scatter
- ParallelCoordinates.svelte -- D3 parallel coordinates
- MethodologyLink.svelte -- "How was this measured?" link
- DownloadButton.svelte -- per-chart CSV/JSON download
- ExplorerFilters.svelte -- filter controls
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chart permalink pages with distribution controls and OG images</name>
  <files>
    platform/src/routes/chart/[slug]/+page.ts,
    platform/src/routes/chart/[slug]/+page.svelte,
    platform/src/lib/components/CitationBlock.svelte,
    platform/src/lib/components/EmbedCodeBlock.svelte,
    platform/src/lib/components/ChartExportButton.svelte,
    platform/src/routes/+page.svelte,
    scripts/generate-og-images.ts
  </files>
  <action>
    Create three reusable components first, then the chart route, then OG image generation:

    **CitationBlock.svelte** - Props: `{ chartTitle: string; slug: string }`. Shows two tabs/toggles: APA and BibTeX. Displays the citation text in a `<pre>` block. Each format has a copy button using `navigator.clipboard.writeText()`. Uses `$state` for `copied` feedback (2s timeout). Also shows platform-level citation below chart citation. Uses design tokens for styling (code block background, border radius). `aria-live="polite"` on copy feedback.

    **EmbedCodeBlock.svelte** - Props: `{ slug: string }`. Computes the iframe embed code string including the current URL query parameters (reads from `window.location.search`). Shows the embed code in a `<pre><code>` block. Copy button with clipboard API. Embed code includes the `<iframe>` tag plus the postMessage resize listener `<script>` per CONTEXT.md locked decision. Uses `import.meta.env.VITE_SITE_URL ?? ''` for the iframe src base URL. In dev, falls back to `window.location.origin`.

    **ChartExportButton.svelte** - Props: `{ chartType: 'svg' | 'plotly'; exportContainerRef?: HTMLElement; svgRef?: SVGSVGElement; plotlyRef?: HTMLElement; plotly?: unknown; slug: string }`.

    CRITICAL (per locked decision): The `exportContainerRef` must point to an outer wrapper `<div>` that contains three elements in order: (1) a title bar with the chart title text, (2) the chart element itself, and (3) an attribution line reading "Henry Baker . Hertie School". The chart page (see +page.svelte below) is responsible for creating this wrapper div and binding it to `exportContainerRef`. The export functions (`exportChartAsPng`, `exportChartAsSvg`) capture this entire wrapper, ensuring the exported image includes the title at top and attribution at bottom - not just the raw chart.

    For svg type: shows "Download PNG" and "Download SVG" buttons. PNG export calls `exportChartAsPng(exportContainerRef, filename)` which captures the full wrapper via html-to-image. SVG export calls `exportChartAsSvg(svgRef, filename)` for the raw SVG, but the SVG export function should be extended in-component to prepend a `<text>` title and append an attribution `<text>` to the cloned SVG before serialisation (or alternatively, wrap the SVG serialisation with title/attribution text in the download blob). For plotly type: shows "Download PNG" only, calls `exportPlotlyPng(plotlyRef, filename, plotly)` - Plotly exports are handled separately since Plotly.toImage captures only the plot div; add title and attribution text to the resulting image by drawing onto a canvas (create canvas, draw title text at top, draw Plotly image in middle, draw attribution at bottom, export canvas as PNG). Filename convention: `llem-{slug}.png` / `llem-{slug}.svg`. Buttons styled with design tokens.

    **+page.ts** - Import `CHART_SLUGS` from chartMeta.ts. Export `entries: EntryGenerator` returning `CHART_SLUGS.map(slug => ({ slug }))`. Export `load: PageLoad` that fetches `fixture-results.json` and `pca-projection.json` (same pattern as explorer/+page.ts) and returns `{ slug: params.slug, allResults, pcaProjection }`.

    **+page.svelte** - The main chart context page. Structure per CONTEXT.md locked decisions:
    1. `<svelte:head>` with per-chart OG/Twitter meta tags using CHART_META and VITE_SITE_URL. OG image: `{siteUrl}/og/{slug}-preview.png`. Twitter card: `summary_large_image`.
    2. PageLayout wrapper with chart title and description from CHART_META.
    3. Read URL query params in `onMount` for filter state (backend, precision, batchSize, attn) using `new URLSearchParams(window.location.search)` per Research pitfall 2 - NOT $page.url.searchParams.
    4. **Export wrapper div** - Create a `<div bind:this={exportContainer}>` that wraps:
       - A title bar: `<div class="export-title">{CHART_META[slug].title}</div>` styled with appropriate font size and padding
       - The chart component (conditionally rendered based on slug)
       - An attribution line: `<div class="export-attribution">Henry Baker . Hertie School</div>` in muted text
       The title and attribution divs should be styled to look clean in the exported image (white background, readable font, centred text). They are visible on the page as natural page elements (the title doubles as the chart heading, the attribution sits below the chart). `bind:this={exportContainer}` on the outer div provides the reference for ChartExportButton.
    5. Conditionally render the correct chart component inside the export wrapper based on `data.slug`:
       - 'heatmap' -> ConfigHeatmapInteractive (pass filteredResults, filter handlers)
       - 'timeseries' -> PowerTimeseries (needs synthesised power curves from data)
       - 'surface' -> Surface3D (pass allResults, filterState)
       - 'pca' -> PCAProjection (pass pcaProjection data, filterState)
       - 'parallel-coords' -> ParallelCoordinates (pass parallelData)
    6. Below the export wrapper: ChartExportButton with `exportContainerRef={exportContainer}` for D3 charts (heatmap/timeseries/parallel-coords), or with plotlyRef for Plotly charts (surface/pca). Use `bind:this` to get SVG and Plotly div references from inside the chart components.
    7. MethodologyLink component.
    8. CitationBlock with chart title and slug.
    9. EmbedCodeBlock with slug.
    10. DownloadButton for filtered data CSV/JSON.
    11. "Back to narrative" link (`/`) and "Explore all charts" link (`/explorer`).

    When filters change, update URL via `history.replaceState()` to keep permalink in sync.

    **Fragment IDs on scroll narrative** - Add `id="heatmap"`, `id="timeseries"` etc. to the relevant beat sections in `platform/src/routes/+page.svelte` (the main scroll page) so URLs like `/#heatmap` scroll to the right section.

    **OG preview images** - Create `scripts/generate-og-images.ts`: a Node.js build script that generates static branded OG preview cards for each chart slug. Approach: use `@vercel/og` or a simple canvas-based approach (node-canvas or sharp with SVG overlay) to produce a 1200x630 PNG per slug. Each image contains: the llem-commons logo/wordmark, the chart title from CHART_META, a brief description, and "Henry Baker . Hertie School" branding. These are text-based branded cards (not chart screenshots) - simple and reliable at build time. Output to `platform/static/og/{slug}-preview.png`. Add a `generate:og` npm script in platform/package.json that runs this script. Run it once to generate the initial set of images and commit them to `platform/static/og/`.
  </action>
  <verify>
    <automated>cd /Users/henrybaker/Repositories/llem-commons/platform && npm run build 2>&1 | tail -20 && ls -la static/og/*.png | wc -l</automated>
  </verify>
  <done>5 chart permalink pages prerender successfully at /chart/heatmap, /chart/timeseries, /chart/surface, /chart/pca, /chart/parallel-coords. Each page shows: chart title (inside export wrapper), interactive chart, attribution line (inside export wrapper), OG meta tags pointing to static preview images, methodology link, citation block (APA + BibTeX with copy), embed code with copy, export buttons (PNG + SVG for D3 charts, PNG for Plotly charts). Exported images include chart title at top and "Henry Baker . Hertie School" attribution at bottom. URL query params restore filter state on load. Fragment IDs added to scroll narrative page. 5 static OG preview images exist in platform/static/og/.</done>
</task>

<task type="auto">
  <name>Task 2: Embed pages with postMessage resize</name>
  <files>
    platform/src/routes/embed/[slug]/+page.svelte,
    platform/src/routes/embed/[slug]/+page.ts
  </files>
  <action>
    **+page.ts** - Identical pattern to chart/[slug]/+page.ts: import CHART_SLUGS, export entries() and load() that fetches fixture-results.json and pca-projection.json. Returns `{ slug, allResults, pcaProjection }`.

    **+page.svelte** - Minimal chrome embed page per CONTEXT.md locked decisions:
    1. No `<svelte:head>` OG tags needed (embeds are in iframes).
    2. NO PageLayout wrapper - this page is minimal. Custom CSS: no margin, no padding on body, clean white background.
    3. Read URL query params in `onMount` using `new URLSearchParams(window.location.search)` for filter state.
    4. Render the chart component based on slug (same conditional as chart page but without any surrounding controls).
    5. Small attribution bar at the bottom: "Henry Baker . Hertie School" in muted text with a link to the chart page. Use design tokens for typography. Bar should be visually minimal (10-12px text, light grey).
    6. ResizeObserver in `onMount`: observe `document.body`, on resize send `window.parent.postMessage({ type: 'llem-resize', height: document.body.scrollHeight }, '*')`. Send initial height immediately. Clean up observer in `onDestroy`.
    7. Apply a `<style>` block scoped to this page: `:global(body) { margin: 0; overflow: hidden; }` to prevent double scrollbars in iframe.

    The embed page should be as lightweight as possible. No citation block, no export buttons, no navigation. Just the chart and attribution.
  </action>
  <verify>
    <automated>cd /Users/henrybaker/Repositories/llem-commons/platform && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>5 embed pages prerender at /embed/heatmap, /embed/timeseries, /embed/surface, /embed/pca, /embed/parallel-coords. Each shows the chart with a small attribution bar. postMessage resize observer sends height to parent frame. URL query params restore filter state. Static build succeeds with all 10 new routes (5 chart + 5 embed) plus existing routes.</done>
</task>

</tasks>

<verification>
cd /Users/henrybaker/Repositories/llem-commons/platform && npm run build && npm run check && npm run lint
</verification>

<success_criteria>
- `npm run build` succeeds and prerendering log shows all 10 new routes (/chart/* and /embed/*)
- Each chart page has og:title, og:description, og:image (pointing to static preview PNG), twitter:card meta tags
- 5 static OG preview images exist in platform/static/og/
- Exported chart images (PNG/SVG) include chart title at top and "Henry Baker . Hertie School" attribution at bottom
- CitationBlock renders APA and BibTeX with functional copy buttons
- EmbedCodeBlock renders iframe code with postMessage script and copy button
- ChartExportButton renders appropriate buttons per chart type and captures the full export wrapper (title + chart + attribution)
- Embed pages have ResizeObserver + postMessage wiring
- Fragment IDs on scroll narrative page enable deep linking
- No lint or type-check errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-distribution-polish/04-02-SUMMARY.md`
</output>
